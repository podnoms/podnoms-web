version: 2.0

jobs:
    login:
        docker:
          - image: circleci/buildpack-deps:18.04-curl-browsers
        steps:
            - run: docker login --username=$DOCKER_USER --password=$DOCKER_PASSWORD $DOCKER_HOST

    build:
        docker:
          - image: circleci/buildpack-deps:18.04-curl-browsers
        steps:
            - checkout
            - run: sudo docker build -t podnoms.azurecr.io/podnoms.web .

    deploy:
        docker:
          - image: circleci/buildpack-deps:18.04-curl-browsers
        steps:
            - run: sudo docker push podnoms.azurecr.io/podnoms.web
workflows:
    version: 2
    commit:
        jobs:
            - login
            - checkout:
                requires:
                  - login
            - build:
                requires:
                  - checkout
                filters:
                    branches:
                      only: master

